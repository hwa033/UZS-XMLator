FLASK_ENV=production
SECRET_KEY=change-me
DOWNLOADS_DIR=/var/www/xml-automation-clean/web/static/downloads
# Add other environment variables below as needed
# Example environment variables for UZS XMLator
# Copy to `.env` and fill real values before running in production
SECRET_KEY=replace_this_with_a_secure_random_value
FLASK_ENV=production
# Path to output map or base output folder (relative to project root)
# If you change OUTPUT_MAP structure, update `web/app.py` accordingly
OUTPUT_BASE=uzs_filedrop
# Logging / debug
LOG_LEVEL=INFO
# Optional: allow embedding design preview when in production
SHOW_FIGMA_EMBED=false
