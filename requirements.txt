# Cleaned runtime requirements (generated from source imports)
# Minimal runtime packages detected as used by the app.
Flask
Flask-Cors
Flask-Limiter
lxml
openpyxl
pywin32
PyYAML
waitress

# Notes:
# - This file is a suggestion. Pin versions before production deployment.
# - If you need Robot Framework test runtime, keep `robotframework` in a separate test requirements file.